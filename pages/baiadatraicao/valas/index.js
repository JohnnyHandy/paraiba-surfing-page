import React from 'react'

import Seed from '../../../components/seed/seed'

const Vala = (props)=>{
    const beachName = 'Praia das Valas (Valas Beach)';
    const description = 'Located in Baia da Traicao'

    return(
        <Seed beachName={beachName} description={description} />
    )
}

export default Vala